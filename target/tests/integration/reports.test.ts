import { describe, it } from "vitest";

describe("Reports tRPC router", () => {
  it.todo("pipelineSummary returns stage counts and totals");

  it.todo("activityVolume returns grouped data for the specified date range");

  it.todo("staleDeals excludes deals in won/lost stages");
});
