/**
 * T139: Integration tests for the scoring pipeline.
 *
 * These tests require database access and are stubbed out as TODOs.
 */

import { describe, it } from "vitest";

describe("scoring pipeline integration", () => {
  it.todo("new activity triggers rescore");

  it.todo("score updates on deal");

  it.todo("batch rescore processes all deals");
});
